# Ruby Traceability

- Ruby is the deterministic source-of-truth side of the next-20 tranche.
- Behavioral changes here must be mirrored into `stakeholder-core` traceability, docs, and downstream packets.
- Downstream repos should cite Ruby file paths and the corresponding `stakeholder-core` rows when porting behavior.
